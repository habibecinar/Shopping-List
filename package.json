{
  "name": "Shopping-List",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "vite build --base=/Shopping-List/",
    "preview": "vite preview",
    "goLive": "npm run dev",
    "goPreview": "npm run build && npm run preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "homepage": "https://habibecinar.github.io/Shopping-List",
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "esbuild": "^0.25.1",
    "simplelightbox": "^2.14.3"
  }
}
